spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

security:
  jwt:
    secret: ${JWT_SECRET}
    expiration-in-ms: ${JWT_EXPIRATION_IN_MS}

  argon2:
    salt-length: 16
    hash-length: 32
    parallelism: 1
    memory: 16384
    iterations: 2
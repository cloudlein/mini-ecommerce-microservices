spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///E:/project/mini-ecommerce-microservices/config-repo
  security:
    user:
      name: config
      password: secret

server:
  port: 8888
